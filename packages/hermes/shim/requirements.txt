# hermes-shim — Hermes /v1 proxy + residual sessions_list compat surface.
# Pinned exact (matching the Hermes supply-chain hygiene policy: no ranges).
fastapi==0.136.1
uvicorn[standard]==0.47.0
httpx==0.28.1
