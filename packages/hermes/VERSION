# Pinned upstream dependency refs for the alfred-black hermes package
# Docker builds. Update these when bumping upstream dependencies and open a PR.
# NOTE: build-hermes.yml passes NO --build-arg — the Dockerfile ARG defaults
# govern the build. Keep this file in sync with those defaults by hand.

# Hermes Agent — the AI runtime.
# https://github.com/NousResearch/hermes-agent
#
# 2026-07-02 (fleet bake): installed from the PyPI WHEEL (==0.17.0), not a git
# ref, so the dashboard `web_dist` bundle ships (the git build omits it). The
# Dockerfile `ARG HERMES_VERSION` default is the source of truth.
# 0.19.0 == 2026-08 bump (was 0.17.0 == fleet bake; 0.14.0 == git v2026.5.16).
# Upgrade 0.17->0.19 (0.18.0/.1/.2 + 0.19.0): no documented breaking changes,
# config schema + CLI stable, adapters reliability-patched. Canaried on home
# before fleet. Rollback: retag :0.17-rollback -> :latest + recreate.
HERMES_VERSION=0.19.0

# openai SDK is pinned to 2.24.0 (Dockerfile ARG OPENAI_VERSION) — the version
# the golden home box runs. Hermes 0.17's floor otherwise floats openai to a
# release that reintroduced the null-output crash. Bump only with a canary.
#
# hermes-relay native-app plugin is VENDORED at
# packages/hermes/plugins/hermes-relay (no upstream tag maps to manifest 1.2.1;
# captured from the golden home box). See that dir's VENDORED.md.

# NOTE: the Alfred vault CLI is no longer fetched at a pinned external SHA.
# It is vendored in-repo at packages/alfred-vault and COPYd into the
# alfred-worker + init images at build time (see those Dockerfiles).
