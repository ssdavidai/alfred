# Pinned upstream dependency refs for the alfred-black hermes package
# Docker builds. Update these when bumping upstream dependencies and open
# a PR. CI reads these values and passes them as --build-arg to docker build.

# Hermes Agent — the AI runtime. Installed via pip from this git tag.
# https://github.com/NousResearch/hermes-agent
# v2026.5.16 == release v0.14.0 (latest at time of writing).
HERMES_REF=v2026.5.16

# NOTE: the Alfred vault CLI is no longer fetched at a pinned external SHA.
# It is vendored in-repo at packages/alfred-vault and COPYd into the
# alfred-worker + init images at build time (see those Dockerfiles).
