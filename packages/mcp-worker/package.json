{
  "name": "@alfred/mcp-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker hosting an MCP server for direct Claude Desktop access to Sir's tenant data. Streamable-HTTP transport. v1 ships single-tenant for david's Sure; multi-tenant routing follows in #772.",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "build": "wrangler deploy --dry-run --outdir dist"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.4.0",
    "@modelcontextprotocol/sdk": "1.29.0",
    "agents": "0.12.3",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250410.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.87.0"
  }
}
