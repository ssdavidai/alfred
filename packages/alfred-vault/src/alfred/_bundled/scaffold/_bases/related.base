filters:
  and:
    - file.hasLink(this.file)
views:
  - type: table
    name: All
    order:
      - file.name
      - type
      - status
      - created
    sort:
      - property: created
        direction: DESC
